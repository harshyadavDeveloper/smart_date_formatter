name: example
description: "A new Flutter project."

publish_to: 'none'

version: 1.0.0+1

config_name:
  name: "Smart Date Formatter"

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: '>=3.10.0'


dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8

  smart_date_formatter:
    path: ../
  rename_app: ^1.6.6

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true